# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(middleware_communicationTaskLib)
ADD_LIBRARY(${MIDDLEWARE_COMMUNICATION_TASKLIB_NAME} SHARED 
    ${MIDDLEWARE_COMMUNICATION_TASKLIB_SOURCES})
add_dependencies(${MIDDLEWARE_COMMUNICATION_TASKLIB_NAME}
    regen-typekit)



TARGET_LINK_LIBRARIES(${MIDDLEWARE_COMMUNICATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${MIDDLEWARE_COMMUNICATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${MIDDLEWARE_COMMUNICATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${MIDDLEWARE_COMMUNICATION_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${MIDDLEWARE_COMMUNICATION_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${MIDDLEWARE_COMMUNICATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${MIDDLEWARE_COMMUNICATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${MIDDLEWARE_COMMUNICATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/middleware_communication)

